export const content = [
  "./src/**/*.{html,js,jsx,ts,tsx}", // Rutas a archivos HTML, JS, JSX, TS, TSX
  "./public/**/*.html",
];
export const theme = {
  extend: {
    // Aquí puedes personalizar los temas, colores, fuentes, etc.
  },
};
export const plugins = [];
